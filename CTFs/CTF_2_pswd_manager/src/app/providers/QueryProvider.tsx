import React from 'react'

export const QueryProvider: React.FC<{ children: React.ReactNode }> = ({ children }) => {
  // Placeholder: integrate React Query or other data client here.
  return <>{children}</>
}
