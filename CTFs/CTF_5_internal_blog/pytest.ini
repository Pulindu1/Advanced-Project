[pytest]
testpaths = tests
markers =
    unit: single-route or single-function contract assertion (in-process, no multi-route flow).
    integration: multi-route flow against real persistence (in-process, isolated fixture).
